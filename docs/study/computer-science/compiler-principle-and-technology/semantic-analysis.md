# 语义分析

## 语义分析程序

* 任务：语义分析程序通过将变量的定义与变量的引用联系起来，对源程序的含义进行检查，检查每一个语法成分是否具有正确的语义。（**收集上下文信息 + 类型检查**）
* 类型检查包括：
    * 动态检查：目标程序运行时进行的检查
    * 静态检查：读入源程序但不执行源程序的情况下进行的检查，由类型检查程序完成，具体包括：
        * 类型检查，e.g.
            * 内部函数 `mod` 的运算对象的类型
            * 表达式中各运算对象的类型
            * 用户自定义函数的参数类型、返回值类型
        * 唯一性检查，e.g.
            * 一个标识符在同一作用域中必须且只能被声明一次
            * `CASE` 语句中用于匹配选择表达式的常量必须各不相同
            * 枚举类型定义中的各元素不允许重复
        * 控制流检查：检查控制语句是否使控制转移到一个合法的位置，e.g.
            * `goto` 语句、`continue` 语句位置是否合法
* 在类型检查后，还要进行语义翻译，输出有语义信息的语法树

<figure markdown="span">
  ![语义分析程序](https://webp-pic.yokumi.cn/2025/12/20251207154245841.png){ loading=lazy width="70%" }
</figure>

## 符号表

符号表用于检查语义（即上下文有关）的正确性，并辅助正确地生成代码。

### 建立和访问时机

<figure markdown="span">
  ![符号表建立与访问时机](https://webp-pic.yokumi.cn/2025/12/20251207161836091.png){ loading=lazy width="70%" }
</figure>

### 符号表操作

**插入**和**检索**。

**1. 要求标识符显式声明的语言（即强类型语言）：**

处理声明语句时，调用两种操作

检索
: 查重、确定新表目的位置

插入
: 建立新的表目

处理名字引用时，调用检索操作

检索
: 查找信息，进行语义分析、代码生成；发现未定义的名字

**2. 允许标识符隐式声明的语言：**

标识符的每次出现都按**首次出现**处理。

检索
: 如果已经声明，进行类型检查；首次出现，插入操作，从其上下文推测出该变量的相关属性

**3. 对于块结构语言（变量区分作用域）的语言：**

还需要**定位**和**重定位**：

定位
: 识别出块（作用域）开始时，执行定位操作：建立一个新的子表，在该块中声明的名字的属性都存放在此子表中

重定位
: 遇到块结束时，执行重定位操作："删除"存放该块中局部名字的子表，这些名字的作用域局部于该块（出了该子表就不能被引用了）

### 符号表组织

#### 块结构语言符号表组织

对于块结构语言，模块中可嵌套子块，且每个块中均可以定义局部变量。每个程序块有一个子表，保存该块中声明的名字及其属性。

##### 栈式符号表

* 当遇到变量声明时，将包含变量属性的记录入栈
* 当到达块结尾时，将该块中声明的所有变量的记录出栈

插入时：检查子表中是否有重名变量

无
: 插入

有
: 报错

检索时：从栈顶到栈底线性检索

若在当前子表中找到
: 则是当前块的局部变量

若在其他子表中找到
: 则是非局部名字

定位：

定位
: 将栈顶指针 `top` 的值压入块索引表顶端；块索引表的元素是指针，指向相应块的子表中第一个记录在栈中的位置

重定位：

重定位
: 用块索引表顶端元素的值恢复栈顶指针 `top`，完成重定位操作；有效地清除刚刚被编译完的块在栈式符号表中的所有记录

## 类型检查

### 数据类型

基本类型
: 即没有内部结构的类型，e.g. `int`, `bool`, `char`

类型构造器
: 即类型运算符。利用类型构造器可以由基本类型构造新的有结构的类型，称为**构造类型**；可以由具有简单结构的构造类型构造新的具有更复杂结构的构造类型

### 类型体制

即类型的规则（类型指派给语法结构的规则），由类型检查程序实现。

### 类型表达式

1. 基本类型是类型表达式
2. 类型名是类型表达式
3. 类型构造器作用于类型表达式的结果仍是类型表达式：

数组
: `array(I,T)` 表示元素类型为 T 和下标集合为 I 的数组的类型表达式，e.g. `array(1...10,integer)`

笛卡尔积
: 如果 $T_1$ 和 $T_2$ 是类型表达式，那么它们的笛卡儿积 $T_1\times T_2$ 也是类型表达式，假定 $\times$ 是左结合的

记录
: 把类型构造器 record 作用于记录中各域类型的笛卡儿积上，就形成了记录的类型表达式。域类型是由域名和域的类型表达式组成的二元组，e.g. $record((i\times int)\times(c\times char)\times(r\times real))$

指针
: 如果 T 是类型表达式，那么 pointer(T) 也是类型表达式，表示类型"指向类型 T 的对象的指针"，e.g. 对于 `row *p`，类型表达式为 `pointer(row)`

函数
: 从定义域类型 D 到值域类型 R 的映射。类型表达式为 $D\rightarrow R$

4. 类型表达式中可以包含变量（称为类型变量），变量的值是类型表达式

### 类型等价

#### 结构等价

两个**类型表达式**结构等价当且仅当它们完全相同。

<figure markdown="span">
  ![结构等价](https://webp-pic.yokumi.cn/2025/12/20251207184936537.png){ loading=lazy width="70%" }
</figure>

#### 名字等价

名字等价的变量，或者它们在同一个声明语句中定义，或者使用相同类型名声明。
